**多源BFS：**
* 虚拟源点：<font color=alice>从每个点到最近的一个起点的距离，其实可以看成这个起点到虚拟源点的最近距离，从虚拟源点求最短路时，会考虑每一个分支，最终会选择一个最近的分支，每个分支对应一个考虑的起点，因此目标一致。也就说加上一个虚拟源点后，就转换为一个单源最短路问题了。</font><font color=blue>在 BFS 中也是使用一个类试的思想，只不过在做的时候，不需要把这个虚拟源点建立出来，只需要在最开始初始化队列的时候，将所有的源点加入队列即可，然后再进行常规的BFS搜索了。</font>